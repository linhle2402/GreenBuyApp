<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundLight">
<!--    ConstraintLayout: Layout  cho phép sắp xếp vị trí linh hoạt bằng ràng buộc (constraint) giữa các view.-->
    <!-- App Bar Layout -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:elevation="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
<!--        AppBarLayout:chứa Toolbar, hỗ trợ hiệu ứng cuộn (scroll behavior).-->
<!--        Được gắn vào đỉnh (top) của ConstraintLayout.-->
<!--        MaterialToolbar: thanh tiêu đề trên cùng, hiển thị chữ "Địa chỉ" và có icon quay lại (ic_back).-->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/white"
            app:navigationIcon="@drawable/ic_back"
            app:navigationIconTint="@android:color/black"
            app:title="Địa chỉ"
            app:titleTextColor="@android:color/black" />
    </com.google.android.material.appbar.AppBarLayout>

<!--    RecyclerView: danh sách cuộn để hiển thị các địa chỉ.-->
<!--    Constraint (ràng buộc) giúp nó:-->
<!--    nằm ngay dưới AppBarLayout-->
<!--    nằm trên nút btn_add_addresss-->
<!--    trải rộng hết chiều ngang (start_toStart, end_toEnd)-->

    <!-- RecyclerView hiển thị danh sách địa chỉ -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewAddress"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/appbar"
        app:layout_constraintBottom_toTopOf="@+id/btn_add_addresss"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Nút thêm địa chỉ mới -->
    <Button
        android:id="@+id/btn_add_addresss"
        android:layout_width="@dimen/_267sdp"
        android:layout_height="@dimen/_51sdp"
        android:text="Thêm địa chỉ mới"
        android:backgroundTint="#2CCE75"
        android:background="@drawable/rounded_button"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:layout_margin="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

<!--    Được gắn constraintBottom_toBottomOf="parent" → nằm dưới cùng màn hình.-->
</androidx.constraintlayout.widget.ConstraintLayout>
